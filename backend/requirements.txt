fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pytest>=8.0.0
httpx>=0.27.0
# macOS: use pybullet-mm (fork with macOS build fixes). Others: official pybullet.
pybullet>=3.2.0; sys_platform != "darwin"
pybullet-mm>=3.2.7.post0; sys_platform == "darwin"
# Phase 2: YOLO perception (optional; first run may download yolov8n.pt).
ultralytics>=8.0.0
